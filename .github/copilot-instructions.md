# Copilot Instructions

## Code Style
Code style is enforced by clippy via `.cargo/clippy.args`.

## Before Committing
Ensure pre-commit hooks are installed: `pre-commit install`

## PR Guidelines
PR format is enforced by CI. Use the template in `.github/PULL_REQUEST_TEMPLATE.md`.
