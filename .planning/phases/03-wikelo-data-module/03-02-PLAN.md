---
phase: 03-wikelo-data-module
plan: 02
type: execute
---

<objective>
Populate WikieloRegistry with static item/source data from Phase 2 research.

Purpose: Load the registry with real Wikelo item data including sources, reliability ratings, and metadata so it's ready for Phase 4 intel integration.
Output: Working registry with 31 items, lookup functions returning real data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/03-wikelo-data-module/03-01-SUMMARY.md

# Data to populate from:
@.planning/phases/02-item-source-research/02-DATA-READY.md

# Types:
@crates/intel/src/wikelo/types.rs

**Tech stack available:** HashMap, Vec, serde, thiserror (all in workspace)
**Established patterns:** Static const arrays for item definitions
**Constraining decisions:**
- Include all 31 items from Phase 2 research
- Low-confidence items (reliability 1-2) included but flagged
- Location naming should be consistent for future route-graph integration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static item definitions module</name>
  <files>crates/wikelo-data/src/items.rs, crates/wikelo-data/src/lib.rs</files>
  <action>
Create `items.rs` with const definitions for all 31 items from 02-DATA-READY.md:

**Creature Parts (10 items):**
- irradiated_valakkar_fang_juvenile (reliability 4)
- irradiated_valakkar_fang_adult (reliability 4)
- irradiated_valakkar_fang_apex (reliability 4)
- irradiated_valakkar_pearl (reliability 4)
- tundra_kopion_horn (reliability 4)
- irradiated_kopion_horn (reliability 2 - LOW CONFIDENCE)
- yormandi_eye (reliability 5)
- yormandi_tongue (reliability 5)
- quasi_grazer_tongue (reliability 3)
- quasi_grazer_egg (reliability 3)

**Mining Materials (5 items):**
- carinite (reliability 5, multiple locations)
- carinite_pure (reliability 2 - LOW CONFIDENCE)
- quantanium (reliability 4)
- copper (reliability 5)
- tungsten (reliability 5)

**Mission/Loot Items (6 items):**
- mg_scrip (reliability 5)
- council_scrip (reliability 2 - LOW CONFIDENCE)
- polaris_bit (reliability 4)
- asd_secure_drive (reliability 5)
- wikelo_favor (reliability 5)
- dchs_05_comp_board (reliability 1 - LOW CONFIDENCE)

**Additional low-confidence items (10 items):**
Add remaining items from 02-DATA-READY.md "Low Confidence Items" table with appropriate placeholder data.

Use function `all_items() -> Vec<WikieloItem>` rather than const array (allows String construction). Import types from intel::wikelo.

Update lib.rs to declare and re-export items module.
  </action>
  <verify>cargo check -p wikelo-data passes; all_items() returns 31 items</verify>
  <done>items.rs exists with 31 item definitions, all_items() compiles</done>
</task>

<task type="auto">
  <name>Task 2: Wire static data into WikieloRegistry::new()</name>
  <files>crates/wikelo-data/src/registry.rs</files>
  <action>
Update WikieloRegistry::new() to load items from items::all_items():

```rust
impl WikieloRegistry {
    /// Create registry with all known Wikelo items.
    pub fn new() -> Self {
        Self::from_items(crate::items::all_items())
    }
}

impl Default for WikieloRegistry {
    fn default() -> Self {
        Self::new()
    }
}
```

Add helper methods for filtering by confidence:
- `high_confidence_items(&self) -> Vec<&WikieloItem>` - reliability >= 4
- `needs_validation(&self) -> Vec<&WikieloItem>` - reliability <= 2

These help consumers distinguish verified data from uncertain data per 03-CONTEXT.md requirements.
  </action>
  <verify>WikieloRegistry::new().item_count() == 31; high_confidence_items returns ~12 items</verify>
  <done>new() loads static data, confidence filtering works</done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests with real data</name>
  <files>crates/wikelo-data/src/registry.rs</files>
  <action>
Add tests that verify real data is loaded correctly:

1. `test_registry_loads_all_items()` - new() returns 31 items
2. `test_valakkar_items_in_pyro()` - items_in_system("Pyro") includes Valakkar items
3. `test_creature_parts_category()` - items_by_category(CreaturePart) returns 10 items
4. `test_stanton_locations()` - all_systems() includes "Stanton"
5. `test_high_confidence_count()` - high_confidence_items returns ~12-22 items
6. `test_needs_validation_count()` - needs_validation returns ~9 items
7. `test_specific_item_lookup()` - get("wikelo_favor") returns correct item with sources

These are integration tests that verify static data is wired correctly.
  </action>
  <verify>cargo test -p wikelo-data -- --nocapture shows all tests passing</verify>
  <done>Integration tests pass, data verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p wikelo-data` succeeds
- [ ] `cargo test -p wikelo-data` passes all tests
- [ ] `cargo clippy -p wikelo-data` has no warnings
- [ ] WikieloRegistry::new() returns registry with 31 items
- [ ] Bidirectional lookups return correct data
- [ ] High-confidence items distinguishable from low-confidence
</verification>

<success_criteria>

- 31 items populated from Phase 2 research
- WikieloRegistry::new() loads all items
- Confidence filtering works (high vs needs validation)
- Integration tests verify real data
- Phase 3 complete, ready for Phase 4 intel integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-wikelo-data-module/03-02-SUMMARY.md`
</output>
