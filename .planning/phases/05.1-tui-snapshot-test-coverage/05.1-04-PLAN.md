---
phase: 05.1-tui-snapshot-test-coverage
plan: 04
type: execute
---

<objective>
Add snapshot tests for map view with Wikelo source highlighting and hotspot panels.

Purpose: map.rs has 0% coverage. The map view includes Wikelo source rings, Wikelo filter mode, and hotspot detail panels with Wikelo item breakdown. Snapshot tests ensure visual regression coverage for these features.
Output: New snapshot tests covering map view rendering with various states.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Prior plan context:
@.planning/phases/05.1-tui-snapshot-test-coverage/05.1-01-PLAN.md

# Source files:
@crates/cli/src/tui/views/map.rs
@crates/cli/src/tui/ui.rs
@crates/cli/src/tui/data/map_locations.rs

**From 05-02 and 05-03:**
- Map view shows locations with Wikelo source rings (magenta)
- 'w' key toggles wikelo_filter (only shows Wikelo source locations)
- Hotspot detail panels show Wikelo items from route origins
- Ring color: LightMagenta for regular, Magenta for high-value

**Test patterns:**
- Need test_app with map_locations containing Wikelo data
- Need hotspots with intersecting routes that have Wikelo origins
- Use 100x30 terminal (map needs more height)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test_app_with_map_data factory</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add a new factory function test_app_with_map_data() that:

  1. Starts from test_app_with_wikelo() base
  2. Sets view = View::Map
  3. Sets map_system = "Stanton"
  4. Populates map_locations with:
     - Star "Stanton" at (0.0, 0.0)
     - Planet "Crusader" at (5.0, 3.0) with wikelo_items: ["Quantanium"], wikelo_high_value: false
     - Station "Port Olisar" at (5.5, 3.2) with wikelo_items: [], wikelo_high_value: false
     - Planet "Hurston" at (-4.0, -2.0) with wikelo_items: ["Irradiated Valakkar Fang", "Council Scrip"], wikelo_high_value: true

  Use MapLocation struct from super::types::MapLocation and MapLocationType enum.

  5. Populates hotspots with 2 RouteIntersection entries:
     - RouteIntersection with point "Port Olisar", 3 routes
     - RouteIntersection with point "Crusader Gateway", 2 routes

  Import route_graph::{RouteIntersection, Coordinate} for hotspot data.

  6. Sets hotspot_limit = 2 so both hotspots show
  </action>
  <verify>cargo test -p cli test_app_with_map_data -- --nocapture compiles</verify>
  <done>test_app_with_map_data() factory function creates App with map locations and hotspots</done>
</task>

<task type="auto">
  <name>Task 2: Add basic map view snapshot test</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add test test_render_map_view():
  1. Create app via test_app_with_map_data()
  2. Create terminal with TestBackend::new(100, 30) - map needs more height
  3. Draw frame and assert_snapshot!

  The snapshot should show:
  - Map canvas with star, planets, station points
  - Wikelo source rings around locations with wikelo_items
  - Bottom panel with hotspot list (2 hotspots)
  - Right panel with selected hotspot detail
  </action>
  <verify>cargo test -p cli test_render_map_view -- --nocapture runs and creates snapshot</verify>
  <done>Base map view snapshot showing Wikelo rings and hotspots</done>
</task>

<task type="auto">
  <name>Task 3: Add Wikelo filter mode snapshot test</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add test test_render_map_view_wikelo_filter():
  1. Create app via test_app_with_map_data()
  2. Set app.wikelo_filter = true
  3. Create terminal with TestBackend::new(100, 30)
  4. Draw frame and assert_snapshot!

  The snapshot should show:
  - Only locations with wikelo_items visible
  - "[WIKELO SOURCES]" indicator in title
  - Locations without Wikelo items hidden
  </action>
  <verify>cargo test -p cli test_render_map_view_wikelo_filter -- --nocapture runs and creates snapshot</verify>
  <done>Wikelo filter mode snapshot showing filtered locations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p cli` passes all tests
- [ ] `cargo insta review` shows 2 new map snapshots
- [ ] Map snapshots show Wikelo rings correctly
- [ ] Wikelo filter snapshot shows filtered view
</verification>

<success_criteria>
- test_app_with_map_data() factory exists with Wikelo-enabled map data
- 2 new snapshot tests for map view (base and wikelo filter)
- Snapshots show correct visual elements
- Phase 5.1 complete after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-tui-snapshot-test-coverage/05.1-04-SUMMARY.md`
</output>
