---
phase: 05.1-tui-snapshot-test-coverage
plan: 01
type: execute
---

<objective>
Add Wikelo-enabled test fixtures and update existing snapshots to include Wikelo data.

Purpose: Current test_app() factory creates targets with wikelo_flag: None. For comprehensive visual regression testing of Wikelo features, we need fixtures that include realistic Wikelo data.
Output: Enhanced test_app_with_wikelo() factory function and updated base snapshots.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Prior phase context (05-01 established Wikelo display patterns):
@.planning/phases/05-tui-wikelo-views/05-01-SUMMARY.md

# Source files:
@crates/cli/src/tui/ui.rs
@crates/intel/src/wikelo/intel.rs
@crates/intel/src/wikelo/types.rs

**Established patterns:**
- Snapshot tests use insta::assert_snapshot! with TestBackend
- test_app() factory creates App with sample data
- SourceFlag has: location, item_count, top_items, has_high_value
- WikieloItemSummary has: name, category, estimated_value
- ItemCategory enum: CreaturePart, MinedMaterial, MissionCurrency, CombatLoot, Equipment, Commodity
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test_app_with_wikelo factory function</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  In the #[cfg(test)] mod tests block, add a new factory function test_app_with_wikelo() that:
  1. Starts from test_app() base
  2. Modifies targets[0] to have wikelo_flag with SourceFlag containing:
     - location: "HDMS-Bezdek" (matches the route origin)
     - item_count: 3
     - top_items: 3 WikieloItemSummary with realistic data (e.g., "Quantanium" MindedMaterial 50000, "Carinite" MinedMaterial 25000, "Irradiated Valakkar Fang" CreaturePart 75000)
     - has_high_value: true
  3. Modifies targets[1] to have wikelo_flag with SourceFlag containing:
     - location: "Port Olisar"
     - item_count: 1
     - top_items: 1 item (e.g., "Council Scrip" MissionCurrency None)
     - has_high_value: false
  4. Also set wikelo_score on routes[0] to Some(85.0) and wikelo_items to vec!["Quantanium", "Carinite"]

  Import intel::{SourceFlag, WikieloItemSummary, ItemCategory} at the top of the test module.
  </action>
  <verify>cargo test -p cli test_app_with_wikelo -- --nocapture compiles (function exists)</verify>
  <done>test_app_with_wikelo() factory function defined and returns App with Wikelo data on targets and routes</done>
</task>

<task type="auto">
  <name>Task 2: Add snapshot tests using Wikelo fixtures</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add new snapshot tests that use test_app_with_wikelo():

  1. test_render_targets_with_wikelo() - targets view showing Wikelo column with "★3" and "1" indicators
  2. test_render_routes_with_wikelo() - routes view showing Wikelo score column with 85

  Each test follows the same pattern as existing tests:
  - Create app via test_app_with_wikelo()
  - Set appropriate view
  - Create terminal with TestBackend::new(100, 25)
  - Draw frame and assert_snapshot!
  </action>
  <verify>cargo test -p cli test_render_targets_with_wikelo test_render_routes_with_wikelo -- --nocapture runs and creates new snapshots</verify>
  <done>2 new snapshot tests created, snapshots generated showing Wikelo column data</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p cli` passes all tests
- [ ] `cargo insta review` shows new snapshots with correct Wikelo column data
- [ ] No clippy warnings in test code
</verification>

<success_criteria>
- test_app_with_wikelo() factory exists and creates App with Wikelo data
- 2 new snapshot tests added (targets_with_wikelo, routes_with_wikelo)
- Snapshots show Wikelo column indicators correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-tui-snapshot-test-coverage/05.1-01-SUMMARY.md`
</output>
