---
phase: 05.1-tui-snapshot-test-coverage
plan: 02
type: execute
---

<objective>
Add snapshot tests for Wikelo detail panel expanded state in targets view.

Purpose: Phase 05-01 added a detail panel that expands when Enter is pressed on a target with wikelo_flag. This panel shows top 5 items with styling. Snapshot tests ensure the layout and content render correctly.
Output: New snapshot tests covering detail panel expanded state with various data scenarios.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Prior plan context:
@.planning/phases/05.1-tui-snapshot-test-coverage/05.1-01-PLAN.md

# Source files:
@crates/cli/src/tui/ui.rs
@crates/cli/src/tui/views/targets.rs

**From 05-01:**
- Detail panel renders via render_wikelo_detail() when target_detail_expanded=true AND selected target has wikelo_flag
- Layout splits 70/30 (table/detail)
- Panel title shows location and "(HIGH VALUE)" suffix when has_high_value=true
- Shows up to 5 items with bold name, category, and value
- Shows "... and N more items" if item_count > 5

**Test patterns:**
- Use test_app_with_wikelo() from 05.1-01
- Set target_detail_expanded = true
- Ensure selected = 0 (first target with high-value Wikelo data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test for high-value detail panel expanded</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add test test_render_targets_detail_expanded_high_value():
  1. Create app via test_app_with_wikelo()
  2. Set app.view = View::Targets
  3. Set app.selected = 0 (first target has high-value Wikelo)
  4. Set app.target_detail_expanded = true
  5. Create terminal and render
  6. assert_snapshot!()

  The snapshot should show:
  - 70% table area with targets
  - 30% detail panel with title " Wikelo Items at HDMS-Bezdek (HIGH VALUE) "
  - 3 item lines with bold names, categories, and values
  </action>
  <verify>cargo test -p cli test_render_targets_detail_expanded_high_value -- --nocapture runs and creates snapshot</verify>
  <done>Snapshot test exists and shows detail panel with high-value indicator and 3 items</done>
</task>

<task type="auto">
  <name>Task 2: Add test for regular (non-high-value) detail panel</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add test test_render_targets_detail_expanded_regular():
  1. Create app via test_app_with_wikelo()
  2. Set app.view = View::Targets
  3. Set app.selected = 1 (second target has regular Wikelo, not high-value)
  4. Set app.target_detail_expanded = true
  5. Create terminal and render
  6. assert_snapshot!()

  The snapshot should show:
  - 70% table area with targets (row 2 highlighted)
  - 30% detail panel with title " Wikelo Items at Port Olisar " (no HIGH VALUE)
  - 1 item line (Council Scrip)
  </action>
  <verify>cargo test -p cli test_render_targets_detail_expanded_regular -- --nocapture runs and creates snapshot</verify>
  <done>Snapshot test exists and shows detail panel without high-value indicator</done>
</task>

<task type="auto">
  <name>Task 3: Add test for many items with overflow indicator</name>
  <files>crates/cli/src/tui/ui.rs</files>
  <action>
  Add helper test_app_with_many_wikelo_items() that creates a target with:
  - item_count: 8
  - top_items: 5 items (the panel only shows 5)
  - has_high_value: true

  Add test test_render_targets_detail_expanded_many_items():
  1. Use the new helper
  2. Expand detail panel
  3. assert_snapshot!()

  The snapshot should show "... and 3 more items" at the bottom.
  </action>
  <verify>cargo test -p cli test_render_targets_detail_expanded_many_items -- --nocapture runs and creates snapshot</verify>
  <done>Snapshot test exists showing overflow indicator "... and 3 more items"</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p cli` passes all tests
- [ ] `cargo insta review` shows 3 new snapshots with correct detail panel layouts
- [ ] High-value panel shows "(HIGH VALUE)" in title
- [ ] Regular panel shows plain title
- [ ] Many-items panel shows overflow indicator
</verification>

<success_criteria>
- 3 new snapshot tests for detail panel states
- All detail panel rendering variations covered
- Snapshots accepted and committed
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-tui-snapshot-test-coverage/05.1-02-SUMMARY.md`
</output>
