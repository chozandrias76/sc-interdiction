---
phase: 09-contract-data-population
plan: 01
type: execute
---

<objective>
Create contracts.toml with all 43 Wikelo contracts and add contract loader to wikelo-data crate.

Purpose: Populate contract data from 09-RESEARCH.md into human-editable TOML format with compile-time embedding.
Output: contracts.toml file with 43 contracts, contracts.rs module with all_contracts() function, tests passing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-contract-data-population/09-RESEARCH.md
@crates/intel/src/wikelo/contracts.rs
@crates/wikelo-data/src/lib.rs
@crates/wikelo-data/src/items.rs

**Prior phase context:**
- Phase 8 defined: ContractCategory, DataConfidence, CurrencyType, ExchangeRate, extended WikieloContract with 7 fields
- WikieloContract struct has #[serde(default)] on new fields, ready for TOML deserialization
- 43 contracts fully researched in 09-RESEARCH.md with all requirements and rewards

**Tech stack:**
- toml crate (0.8.x) for TOML parsing with serde
- once_cell::sync::Lazy for lazy initialization (already in project)
- include_str!() macro for compile-time embedding

**TOML format (from research):**
```toml
[[contracts]]
id = "new_to_system"
name = "New to System"
category = "Prerequisite"
confidence = "Confirmed"
repeatable = false
available = true
prerequisites = []
turn_in_locations = ["Wikelo Emporium Kinga", "Wikelo Emporium Dasi", "Wikelo Emporium Selo"]

[[contracts.requirements]]
item_id = "vestal_water"
quantity = 1

[[contracts.rewards]]
name = "Access to Wikelo Contracts"
reward_type = "Access"
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toml dependency and create contract data module</name>
  <files>crates/wikelo-data/Cargo.toml, crates/wikelo-data/src/contracts.rs, crates/wikelo-data/src/lib.rs</files>
  <action>
1. Add `toml = "0.8"` and `once_cell = "1.20"` to wikelo-data/Cargo.toml dependencies
2. Create crates/wikelo-data/src/contracts.rs with:
   - `const CONTRACTS_TOML: &str = include_str!("../data/contracts.toml");`
   - Wrapper struct `ContractsFile { contracts: Vec<WikieloContract> }` for TOML root
   - Static `CONTRACTS: Lazy<Vec<WikieloContract>>` that parses TOML once
   - `pub fn all_contracts() -> &'static [WikieloContract]`
   - `pub fn get_contract(id: &str) -> Option<&'static WikieloContract>`
3. Update lib.rs to export contracts module: `pub mod contracts; pub use contracts::{all_contracts, get_contract};`
4. Create empty data/contracts.toml to allow initial compilation

Use intel::wikelo::contracts::WikieloContract import. Pattern: match items.rs lazy loading style.
  </action>
  <verify>cargo check -p wikelo-data compiles (with empty TOML)</verify>
  <done>contracts.rs exists with all_contracts/get_contract functions, lib.rs exports them, empty TOML file exists</done>
</task>

<task type="auto">
  <name>Task 2: Populate contracts.toml with all 43 contracts</name>
  <files>crates/wikelo-data/data/contracts.toml</files>
  <action>
Populate contracts.toml with all 43 contracts from 09-RESEARCH.md. Use exact data from research:

**Contract categories (by count):**
- Prerequisite: 1 (new_to_system)
- FavorExchange: 5 (turn_things_to_favor, trade_council_scrip, need_mining_things, trade_worm_parts, very_hungry)
- Weapon: 15 (yormandi_gun, fix_up_coda_gun, f55_look_better, fun_kopion_skull_gun, fun_kopion_tooth_gun, fun_military_skull_gun, fun_military_tooth_gun, heavy_volt_zapper, make_gun_sandy, make_volt_shotgun_angrier, prettify_karna_gun, snow_snipe, volt_gun_more_navy, zappy_gun_more_woodlike, need_ore_will_give_guns)
- Armor: 9 (armor_with_horn_and_string, hide_snow_suit, look_at_desert, make_glowy_armor, make_space_navy_armor, test_armor, walk_in_danger_look_good, want_armor_look_like_tree, xian_xanthule_suit_better)
- Ship: 9 (adventure_a_aventure, have_good_ships_for_trade, make_atls_shoot, make_jumpy_atls_shoot, now_make_polaris, turn_atls_suit_pretty, very_very_nice_ship, want_big_ship, want_polaris)
- Equipment: 4 (trade_for_useful_ship_parts, trade_for_very_useful_ship_parts, want_super_useful_ship_parts, want_better_eyes)

For each contract include:
- id, name, category, confidence (all "Confirmed"), repeatable (true except new_to_system), available (true, except need_mining_things = false)
- prerequisites = ["new_to_system"] for all except new_to_system itself
- turn_in_locations = ["Wikelo Emporium Kinga", "Wikelo Emporium Dasi", "Wikelo Emporium Selo"]
- requirements array with item_id and quantity
- rewards array with name and reward_type

Use snake_case for item_ids even if items don't exist yet in registry (Plan 09-02 adds missing items).
Set limited_time = true only for now_make_polaris contract.
  </action>
  <verify>cargo test -p wikelo-data (TOML parses correctly, 43 contracts loaded)</verify>
  <done>contracts.toml has 43 valid [[contracts]] entries matching 09-RESEARCH.md data</done>
</task>

<task type="auto">
  <name>Task 3: Add contract loader tests</name>
  <files>crates/wikelo-data/src/contracts.rs</files>
  <action>
Add test module to contracts.rs with:

1. `test_all_contracts_parse()` - Verify 43 contracts load successfully
2. `test_all_contracts_have_unique_ids()` - No duplicate IDs
3. `test_prerequisite_contract_exists()` - new_to_system is present
4. `test_contracts_by_category_count()` - Verify category distribution (1 Prerequisite, 5 FavorExchange, 15 Weapon, 9 Armor, 9 Ship, 4 Equipment)
5. `test_favor_exchange_contracts_have_confirmed_confidence()` - Key contracts have Confirmed data
6. `test_polaris_is_limited_time()` - now_make_polaris has limited_time = true

Use #[cfg(test)] module pattern. All tests should pass.
  </action>
  <verify>cargo test -p wikelo-data -- --include-ignored contracts passes all tests</verify>
  <done>6 contract loading tests passing, cargo clippy -p wikelo-data clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p wikelo-data` succeeds
- [ ] `cargo test -p wikelo-data` passes all tests (including 6 new contract tests)
- [ ] `cargo clippy -p wikelo-data` has no warnings
- [ ] contracts.toml has exactly 43 [[contracts]] entries
- [ ] all_contracts() returns 43 contracts
</verification>

<success_criteria>
- All 3 tasks completed
- contracts.toml with 43 Wikelo contracts (matching 09-RESEARCH.md)
- contracts.rs with all_contracts() and get_contract() functions
- 6+ unit tests verifying contract loading
- No clippy warnings, all tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/09-contract-data-population/09-01-SUMMARY.md`
</output>
