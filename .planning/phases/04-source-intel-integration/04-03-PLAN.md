---
phase: 04-source-intel-integration
plan: 03
type: execute
---

<objective>
Add Wikelo scoring to route and hotspot calculations.

Purpose: Incorporate Wikelo item value into route ranking and hotspot identification
Output: Enhanced HotRoute and InterdictionHotspot with Wikelo scoring
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./04-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-source-intel-integration/04-02-SUMMARY.md

**Tech stack available:**
- WikieloIntel integrated into TargetAnalyzer
- SourceFlag with item_count, has_high_value
- HotRoute with risk_score, estimated_haul_value
- InterdictionHotspot with total_cargo_value

**Established patterns:**
- Risk scores 0-100 scale
- OrderedFloat for sorting by f64 values
- Optional fields for backward compatibility

**Key files:**
@crates/intel/src/targets.rs
@crates/intel/src/wikelo/intel.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wikelo_score to HotRoute</name>
  <files>crates/intel/src/targets.rs</files>
  <action>
Enhance HotRoute struct and get_hot_routes():

1. Add field: `wikelo_score: Option<f64>` (0-100 scale, None if no Wikelo integration)
2. Add field: `wikelo_items: Vec<String>` (names of Wikelo items at origin, empty if none)

In get_hot_routes():
- If self.wikelo is Some, calculate wikelo_score for each route's origin:
  - Base: 20 points if is_wikelo_source(origin)
  - +10 per high-value item (estimated_value > 10000)
  - +5 per item up to 50 points
  - Cap at 100
- Populate wikelo_items with item names from flag_location()

This score represents "how likely is cargo from this origin to include Wikelo items".
  </action>
  <verify>cargo check -p intel compiles</verify>
  <done>HotRoute has wikelo_score and wikelo_items fields, populated in get_hot_routes</done>
</task>

<task type="auto">
  <name>Task 2: Add wikelo_potential to InterdictionHotspot</name>
  <files>crates/intel/src/targets.rs</files>
  <action>
Enhance InterdictionHotspot struct and get_interdiction_hotspots():

1. Add field: `wikelo_potential: Option<f64>` (0-100 scale)
2. Add field: `wikelo_items: Vec<String>` (Wikelo items available at this location)

In get_interdiction_hotspots():
- If self.wikelo is Some, calculate wikelo_potential:
  - Call flag_location() for each hotspot location
  - Use same scoring as HotRoute wikelo_score
- Populate wikelo_items from the flag

This enhances hotspot ranking with Wikelo intelligence.
  </action>
  <verify>cargo check -p intel compiles</verify>
  <done>InterdictionHotspot has wikelo_potential and wikelo_items, populated in get_interdiction_hotspots</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and verify Phase 4 complete</name>
  <files>crates/intel/src/targets_tests.rs</files>
  <action>
Add tests for Wikelo-enhanced scoring:

1. test_hot_route_wikelo_score - routes from Wikelo sources have non-zero score
2. test_hotspot_wikelo_potential - hotspots at Wikelo sources have non-zero potential
3. test_wikelo_score_without_integration - verify None/empty when no WikieloIntel

Run full test suite and clippy to verify Phase 4 integration is complete.
  </action>
  <verify>cargo test -p intel --lib passes, cargo clippy -p intel clean</verify>
  <done>3 new tests pass, Phase 4 complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p intel` succeeds
- [ ] `cargo test -p intel --lib` passes all tests
- [ ] `cargo clippy -p intel` has no new warnings
- [ ] HotRoute and InterdictionHotspot have Wikelo scoring
</verification>

<success_criteria>

- HotRoute has wikelo_score and wikelo_items
- InterdictionHotspot has wikelo_potential and wikelo_items
- Scoring algorithm produces 0-100 values based on Wikelo item presence
- Backward compatible (no WikieloIntel = None/empty)
- 3 new tests pass
- Phase 4 complete: Wikelo source flagging integrated into intel crate
</success_criteria>

<output>
After completion, create `.planning/phases/04-source-intel-integration/04-03-SUMMARY.md`

Update STATE.md: Phase 4 complete, ready for Phase 5 (TUI Wikelo Views)
</output>
