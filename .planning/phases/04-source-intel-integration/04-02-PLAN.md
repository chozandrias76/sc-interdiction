---
phase: 04-source-intel-integration
plan: 02
type: execute
---

<objective>
Integrate WikieloIntel source flagging into TargetAnalyzer.

Purpose: Enhance target predictions with Wikelo source intelligence
Output: TargetAnalyzer methods that flag targets based on Wikelo item sources
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./04-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-source-intel-integration/04-01-SUMMARY.md

**Tech stack available:**
- WikieloIntel from 04-01 with source flagging methods
- TargetAnalyzer with predict_targets_at, get_hot_routes, get_interdiction_hotspots

**Established patterns:**
- TargetAnalyzer holds Arc<ShipRegistry>
- Arc sharing for registry singletons
- Stateless service consuming registries

**Key files:**
@crates/intel/src/targets.rs
@crates/intel/src/wikelo/intel.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WikieloIntel to TargetAnalyzer</name>
  <files>crates/intel/src/targets.rs</files>
  <action>
Modify TargetAnalyzer to optionally hold WikieloIntel:

1. Add field: `wikelo: Option<Arc<WikieloIntel>>`
2. Modify `new()` to accept optional wikelo parameter
3. Add `with_wikelo(self, wikelo: Arc<WikieloIntel>) -> Self` builder method
4. Add `wikelo(&self) -> Option<&WikieloIntel>` accessor

Keep backward compatible - existing code that doesn't use Wikelo still works.
  </action>
  <verify>cargo check -p intel compiles, existing tests pass</verify>
  <done>TargetAnalyzer has optional WikieloIntel field with accessor</done>
</task>

<task type="auto">
  <name>Task 2: Add wikelo_flag to TargetPrediction</name>
  <files>crates/intel/src/targets.rs</files>
  <action>
Enhance TargetPrediction struct:

1. Add field: `wikelo_flag: Option<SourceFlag>` (import from wikelo module)
2. Modify `predict_targets_at()` to populate wikelo_flag:
   - If self.wikelo is Some, call wikelo.flag_location(destination) for departing targets
   - For arriving targets, the cargo is already on the ship (not useful for source flagging)
   - Only flag departing targets from Wikelo source locations

This tells interdictors: "targets departing from X are likely carrying these Wikelo items".
  </action>
  <verify>cargo check -p intel compiles, cargo test -p intel --lib passes</verify>
  <done>TargetPrediction includes wikelo_flag populated for departing targets from Wikelo sources</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for Wikelo-enhanced predictions</name>
  <files>crates/intel/src/targets_tests.rs</files>
  <action>
Add integration tests for Wikelo-enhanced TargetAnalyzer:

1. test_predict_targets_without_wikelo - verify backward compatibility (wikelo_flag is None)
2. test_predict_targets_with_wikelo_departing - departing targets from Wikelo source get flag
3. test_predict_targets_with_wikelo_arriving - arriving targets don't get flag (not useful)

Use mock UexClient if needed, or filter to known locations.
Test with real WikieloRegistry data to verify actual source locations are flagged.
  </action>
  <verify>cargo test -p intel --lib passes all tests including new ones</verify>
  <done>3 new tests verify Wikelo integration with TargetAnalyzer</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p intel` succeeds
- [ ] `cargo test -p intel --lib` passes all tests
- [ ] `cargo clippy -p intel` has no new warnings
- [ ] TargetAnalyzer with Wikelo produces flagged predictions
</verification>

<success_criteria>

- TargetAnalyzer optionally holds WikieloIntel
- TargetPrediction has wikelo_flag field
- Departing targets from Wikelo sources get populated flags
- Backward compatible (no Wikelo = no flag, works as before)
- 3 new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-source-intel-integration/04-02-SUMMARY.md`
</output>
