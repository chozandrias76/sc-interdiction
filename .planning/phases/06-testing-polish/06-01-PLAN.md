---
phase: 06-testing-polish
plan: 01
type: execute
---

<objective>
Implement the stubbed integration tests for TargetAnalyzer API composite methods.

Purpose: Enable full end-to-end testing of TargetAnalyzer methods that depend on UexClient.
Output: Working integration tests for get_hot_routes, predict_targets_at, get_trade_runs, and get_interdiction_hotspots.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/intel/tests/target_analyzer_integration.rs
@crates/api-client/src/uex.rs
@crates/api-client/src/uex_tests.rs
@crates/intel/src/targets.rs

**Prior work:**
- Phase 5.2 added extensive unit tests for helper functions
- uex_tests.rs shows mockito patterns for API mocking
- Integration tests are currently stubbed with #[ignore]

**Key insight:**
The integration tests require mocking both /commodities_prices_all and /terminals
endpoints that get_trade_routes() calls. The mock responses must match the UEX API
schema and provide realistic data for the business logic to process.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mock response fixtures for composite API testing</name>
  <files>crates/intel/tests/fixtures.rs</files>
  <action>
Create a fixtures module with helper functions that generate mock JSON responses:

1. Create `crates/intel/tests/fixtures.rs` with:
   - `mock_commodities_prices_all_response()` - returns JSON matching UEX commodities_prices_all schema
   - `mock_terminals_response()` - returns JSON matching UEX terminals schema
   - Use realistic data: 2-3 commodities, 3-4 terminals across Stanton/Pyro
   - Include price differentials to create profitable routes

2. Update `crates/intel/tests/target_analyzer_integration.rs`:
   - Add `mod fixtures;` import
   - Remove #[ignore] attributes from tests

Schema references from uex_tests.rs:
- commodities_prices_all: {data: [{id_commodity, commodity_name, commodity_code, id_terminal, terminal_name, price_buy, price_sell, scu_buy, scu_sell, ...}]}
- terminals: {data: [{id, name, code, system, ...}]}

DO NOT create complex nested structures - keep fixtures minimal but realistic.
  </action>
  <verify>cargo test -p intel --test target_analyzer_integration -- --list shows tests without [ignore]</verify>
  <done>Fixture module exists with mock response helpers, tests no longer ignored</done>
</task>

<task type="auto">
  <name>Task 2: Implement integration tests with composite API mocking</name>
  <files>crates/intel/tests/target_analyzer_integration.rs</files>
  <action>
Implement the four integration tests using mockito server:

1. `test_get_hot_routes_integration`:
   - Mock /commodities_prices_all and /terminals endpoints
   - Create UexClient with mock server URL
   - Call analyzer.get_hot_routes(10)
   - Assert returns routes sorted by profit
   - Assert routes contain expected commodities

2. `test_predict_targets_at_integration`:
   - Mock same endpoints
   - Call analyzer.predict_targets_at("Port Olisar", routes) with mock routes
   - Assert predictions are classified as ARRIVING/DEPARTING
   - Assert ship estimates are reasonable

3. `test_get_trade_runs_integration`:
   - Mock same endpoints
   - Call analyzer.get_trade_runs(5)
   - Assert returns round-trip trade runs
   - Assert outbound and return legs are populated

4. `test_get_interdiction_hotspots_integration`:
   - Mock same endpoints
   - Call analyzer.get_interdiction_hotspots(3, false)
   - Assert returns hotspots with route counts
   - Assert wikelo_potential is populated when WikieloIntel configured

Follow the mockito patterns from uex_tests.rs (Server::new_async, mock().create_async).
Use fixtures from Task 1 for response bodies.
  </action>
  <verify>cargo test -p intel --test target_analyzer_integration passes all 4 tests</verify>
  <done>All 4 integration tests pass, testing full API→analysis flow</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p intel --test target_analyzer_integration` passes all tests
- [ ] No #[ignore] attributes remain on integration tests
- [ ] `cargo clippy -p intel` has no warnings
</verification>

<success_criteria>
- All 4 integration tests implemented and passing
- Tests cover the composite API→analysis flow
- Fixtures are minimal but realistic
- No ignored tests remain
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-polish/06-01-SUMMARY.md`
</output>
