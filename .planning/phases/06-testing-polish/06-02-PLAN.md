---
phase: 06-testing-polish
plan: 02
type: execute
---

<objective>
Add inline documentation to undocumented public types and finalize edge case coverage.

Purpose: Improve code discoverability and ensure critical edge cases are tested.
Output: Comprehensive doc comments on all public API types and methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/intel/src/targets.rs
@crates/intel/src/ships/types.rs
@crates/intel/src/ships/enrichment.rs
@crates/intel/src/wikelo/types.rs

**Documentation gaps identified:**
- HotRoute, TradeRun, RouteLeg, InterdictionHotspot missing field docs
- CargoShip struct has minimal field docs
- ShipRole variants need descriptions
- Some enrichment functions lack doc comments

**Edge cases to verify:**
- Empty trade routes response handling
- Zero-profit route filtering
- Ship estimation with unknown ships
- Wikelo scoring with no items
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline documentation to intel types</name>
  <files>crates/intel/src/targets.rs, crates/intel/src/ships/types.rs</files>
  <action>
Add doc comments to undocumented public structs and their fields:

In targets.rs:
1. Add field docs to InterdictionHotspot (routes_through, volume, top_commodities, ship_frequencies, etc.)
2. Add field docs to CommodityValue (name, total_value)
3. Add field docs to ShipFrequency (name, count, avg_cargo)
4. Document calculate_wikelo_score() helper function

In ships/types.rs:
1. Add descriptions to ShipRole variants (Hauler, Miner, Explorer, etc.)
2. Add field docs to CargoShip (name, role, cargo_capacity, quantum_fuel_capacity, etc.)
3. Add field docs to SalvageValue (hull_materials, components, cargo_potential, total_estimate)
4. Add field docs to LootEstimate (cargo_estimate, salvage_value, total_estimate)

Keep doc comments concise - one line per field. Use "///".
Follow existing patterns in the codebase.
  </action>
  <verify>cargo doc -p intel --no-deps builds without warnings</verify>
  <done>All public types have doc comments, cargo doc generates clean output</done>
</task>

<task type="auto">
  <name>Task 2: Add edge case tests for empty/error conditions</name>
  <files>crates/intel/src/targets_tests.rs</files>
  <action>
Add tests for edge cases not yet covered:

1. `test_estimate_ship_unknown_ship`:
   - Call estimate_ship_for_route with an unknown ship name
   - Assert returns None or default estimation

2. `test_get_hot_routes_empty_input`:
   - Create test with empty trade routes
   - Assert returns empty result without panic

3. `test_calculate_wikelo_score_no_items`:
   - Location with WikieloIntel but no items at location
   - Assert score is 0.0, items is empty vec

4. `test_parse_terminal_with_missing_system`:
   - Terminal string without system suffix
   - Assert graceful handling (uses empty string or default)

Keep tests minimal - single assertion per concept.
Place in existing #[cfg(test)] mod tests block in targets_tests.rs.
  </action>
  <verify>cargo test -p intel targets_tests passes including new tests</verify>
  <done>Edge case tests added and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo doc -p intel --no-deps` builds without warnings
- [ ] `cargo test -p intel` passes all tests
- [ ] `cargo clippy -p intel` has no warnings
- [ ] All public types have doc comments
</verification>

<success_criteria>
- All public structs, enums, and their fields have doc comments
- Edge case tests for empty/error conditions pass
- No documentation warnings from cargo doc
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-polish/06-02-SUMMARY.md`

Final step: Update ROADMAP.md to mark Phase 6 complete.
</output>
