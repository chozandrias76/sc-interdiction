---
phase: 12-cli-tui-demand-views
plan: 02
type: execute
---

<objective>
Add demand display to TUI views alongside existing Wikelo source display.

Purpose: Show contract demand info in TUI to complete the supply/demand intel picture.
Output: Demand indicators in targets view, routes view, and updated detail panel.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-cli-tui-demand-views/12-01-SUMMARY.md
@.planning/phases/11-demand-intel-integration/11-01-SUMMARY.md
@.planning/phases/05-tui-wikelo-views/05-01-SUMMARY.md

# Key source files:
@crates/cli/src/tui/views/targets.rs
@crates/cli/src/tui/views/routes.rs
@crates/cli/src/tui/app.rs
@crates/intel/src/wikelo/intel.rs

**Tech stack available:**
- TargetPrediction.demand_flag (Option<DemandFlag>)
- HotRoute.demand_score (Option<f64>), demand_contracts (Vec<String>)
- InterdictionHotspot.demand_score, demand_contracts
- DemandFlag has: location, contract_count, top_contracts, has_high_value
- DemandContractSummary has: name, category, reward_value

**Established patterns:**
- Wikelo source column shows "★N" for high-value, "N" for regular, "-" for none
- Detail panel splits layout 70/30 when expanded
- Enter key toggles detail expansion
- Colors: Yellow for high-value, Green for regular, DarkGray for none
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Demand column to targets view</name>
  <files>crates/cli/src/tui/views/targets.rs</files>
  <action>
Add "Demand" column to targets table (parallel to existing "Wikelo" column):

1. Add header cell: Cell::from("Demand").style(Style::default().fg(Color::Yellow))

2. Add demand indicator in row rendering (after wikelo_text):
   - Check target.demand_flag
   - If Some with has_high_value: format "★{contract_count}", Color::Cyan
   - If Some without high_value: format "{contract_count}", Color::LightCyan
   - If None: "-", Color::DarkGray

3. Update widths array to include new column (Length(8) for Demand)

4. Update detail panel logic:
   - If demand_flag is present and target_detail_expanded, show demand detail
   - Create render_demand_detail() function parallel to render_wikelo_detail()
   - Shows top contracts with name, category, reward value

Column order: Dir | Ship | Cargo | Destination | Wikelo | Demand | Value | Threat

Keep color scheme distinct from source (Cyan for demand vs Yellow/Green for source).
  </action>
  <verify>
cargo build --package sc-interdiction && cargo test --package sc-interdiction
cargo clippy --package sc-interdiction passes
Snapshot tests may need updating (run cargo insta review if using insta)
  </verify>
  <done>
Demand column visible in targets table.
Targets with demand_flag show contract count indicator.
Detail panel shows demand contracts when expanded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Demand column to routes view</name>
  <files>crates/cli/src/tui/views/routes.rs</files>
  <action>
Add demand score display to routes table:

1. Add header cell: Cell::from("Demand").style(Style::default().fg(Color::Yellow))

2. Add demand indicator in row rendering (after wikelo_text):
   - Check route.demand_score
   - If Some(s) where s > 50.0: format "★{:.0}", Color::Cyan
   - If Some(s) where s > 20.0: format "{:.0}", Color::LightCyan
   - If Some(s) where s > 0.0: format "{:.0}", Color::DarkGray
   - Otherwise: "-", Color::DarkGray

3. Update widths array to include new column (Length(7) for Demand)

Column order: Commodity | Origin | Destination | Profit/SCU | Haul Value | Wikelo | Demand | Ship

Parallel the existing Wikelo score display pattern but with Cyan colors for demand.
  </action>
  <verify>
cargo build --package sc-interdiction
cargo clippy --package sc-interdiction passes
cargo test --package sc-interdiction passes
  </verify>
  <done>
Demand score column visible in routes view.
Routes with demand at destination show score indicator.
Color coding consistent with targets view.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update snapshot tests for new columns</name>
  <files>crates/cli/src/tui/snapshots/*.snap</files>
  <action>
Run the TUI snapshot tests and update any failing snapshots:

1. Run cargo test --package sc-interdiction to see which snapshots fail
2. For each failing snapshot, review the diff
3. If the only change is the new Demand column, accept with cargo insta review
4. If tests use inline expect, update the expected output manually

Also update any test helpers that construct mock TargetPrediction or HotRoute:
- Ensure demand_flag/demand_score/demand_contracts are set to reasonable defaults
- Check crates/cli/src/tui/ui.rs for test helper functions

This is cleanup/verification - no new logic needed.
  </action>
  <verify>
cargo test --package sc-interdiction -- --include-ignored passes
All snapshot tests pass without pending reviews
  </verify>
  <done>
All TUI tests pass.
Snapshots updated to reflect new Demand column.
No regressions in existing functionality.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --package sc-interdiction` succeeds
- [ ] `cargo clippy --package sc-interdiction` passes
- [ ] `cargo test --package sc-interdiction` passes (all 15+ TUI tests)
- [ ] Targets view shows Demand column with contract counts
- [ ] Routes view shows Demand score column
- [ ] Detail panel can show demand contract info
- [ ] Color scheme distinguishes demand (cyan) from source (yellow/green)
</verification>

<success_criteria>

- Demand column added to targets view
- Demand column added to routes view
- Detail panel shows demand contracts
- All tests pass including snapshots
- Phase 12 complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-cli-tui-demand-views/12-02-SUMMARY.md`
</output>
