---
phase: 05-tui-wikelo-views
plan: 01
type: execute
---

<objective>
Add Wikelo intelligence column to targets view with expandable detail.

Purpose: Let interdictors quickly identify high-Wikelo-value targets and drill into item specifics.
Output: Targets table with Wikelo score column and expanded detail panel showing item cards.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tui-wikelo-views/05-CONTEXT.md
@.planning/phases/04-source-intel-integration/04-03-SUMMARY.md

# Key source files:
@crates/cli/src/tui/views/targets.rs
@crates/cli/src/tui/app.rs
@crates/cli/src/tui/types.rs
@crates/intel/src/targets.rs (TargetPrediction with wikelo_flag)
@crates/intel/src/wikelo/intel.rs (SourceFlag, WikieloItemSummary)

**Tech stack available:** ratatui (Table, Cell, Row, Paragraph, Block)
**Established patterns:**
- Table with header row and data rows (see targets.rs)
- Color-coded indicators (threat bar pattern)
- Detail expansion toggle (detail_expanded in map view)

**Constraining decisions:**
- [Phase 4-01]: TargetPrediction.wikelo_flag is Option<SourceFlag>
- [Phase 4-02]: Only departing targets get wikelo_flag (arriving have cargo on ship already)

**From CONTEXT.md:**
- Score column for quick scanning
- Full item cards when drilling in: name, category, source type, price if available
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wikelo score column to targets table</name>
  <files>crates/cli/src/tui/views/targets.rs, crates/cli/src/tui/types.rs</files>
  <action>
Add a "Wikelo" column between Destination and Value columns in the targets table.

1. Add header cell "Wikelo" with yellow style (matches other headers)
2. For each target row, render wikelo indicator:
   - If `target.wikelo_flag.is_some()`: Show score bar or emoji based on item count
   - High value (has_high_value=true): Use Color::Yellow with "★" prefix
   - Regular source: Use Color::Green with item_count display (e.g., "3 items")
   - If `wikelo_flag.is_none()`: Show "-" in DarkGray
3. Add column width constraint (Length(10))
4. Update widths array to accommodate new column

Pattern from existing threat column:
```rust
let threat_bar = "█".repeat(count).to_string() + &"░".repeat(max - count);
```

For Wikelo, simpler display: "★3" for high-value with 3 items, "3" for regular, "-" for none.

Do NOT add sorting by Wikelo in this task - focus on display only.
  </action>
  <verify>cargo build --package cli passes. TUI compiles with new column visible.</verify>
  <done>Targets table shows Wikelo column with score indicator for each target.</done>
</task>

<task type="auto">
  <name>Task 2: Add Wikelo detail expansion to targets view</name>
  <files>crates/cli/src/tui/views/targets.rs, crates/cli/src/tui/app.rs, crates/cli/src/tui/handlers/targets.rs</files>
  <action>
Add expandable detail panel that shows Wikelo item cards when a target with wikelo_flag is selected.

1. In app.rs: Add `target_detail_expanded: bool` field (default false)
2. In handlers/targets.rs: Toggle on Enter key when selected target has wikelo_flag
3. In targets.rs render function:
   - When target_detail_expanded is true and selected target has wikelo_flag:
   - Split area vertically: table 70%, detail panel 30%
   - Render detail panel with Block titled "Wikelo Items"
   - For each item in wikelo_flag.top_items (up to 5):
     - Show item card: Name (bold), Category, Value if present
     - Format: "Irradiated Valakkar Fang (Creature Part) ~15,000 aUEC"
   - Show total item_count at bottom if more than displayed

Detail panel layout (Paragraph with styled Lines):
```rust
Line::from(vec![
    Span::styled(&item.name, Style::default().add_modifier(Modifier::BOLD)),
    Span::raw(format!(" ({:?})", item.category)),
    // Value if present
]);
```

4. Add keyboard hint in table title when wikelo target selected: "(Enter: details)"

Do NOT change filtering or sorting - focus on detail display only.
  </action>
  <verify>cargo build --package cli passes. Enter key toggles detail panel for Wikelo targets.</verify>
  <done>Selecting a Wikelo target and pressing Enter shows item cards panel. Enter again hides it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --package cli` succeeds without errors
- [ ] `cargo clippy --package cli` passes with no new warnings
- [ ] TUI launches and shows Wikelo column in targets view
- [ ] Targets with wikelo_flag show score indicator
- [ ] Targets without wikelo_flag show "-"
- [ ] Enter key toggles detail panel for Wikelo targets
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Wikelo column visible in targets table
- Detail panel shows item cards for Wikelo targets
- No TypeScript errors or clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/05-tui-wikelo-views/05-01-SUMMARY.md`
</output>
