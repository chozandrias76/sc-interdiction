---
phase: 05.2-coverage-to-80
plan: 02
type: execute
---

<objective>
Add unit tests to intel crate for ships/registry.rs and wikelo/contracts.rs modules.

Purpose: These modules have pure functions with low or zero coverage that are easily testable.
Output: ~60+ additional lines covered in intel crate.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@crates/intel/src/ships/registry.rs
@crates/intel/src/wikelo/contracts.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ships/registry.rs unit tests</name>
  <files>crates/intel/src/ships/registry.rs</files>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of registry.rs for the lookup functions.

**Helper function:**
Create `mock_cargo_ship(name: &str, cargo: u32)` helper that returns a CargoShip with minimal required fields.

**Tests:**
- `test_find_by_name_exact_match`: Find "Caterpillar" returns correct ship
- `test_find_by_name_case_insensitive`: Find "caterpillar" (lowercase) finds "Caterpillar"
- `test_find_by_name_not_found`: Find "NonExistent Ship" returns None
- `test_find_by_min_cargo_filters`: Ships with cargo >= threshold returned
- `test_find_by_min_cargo_empty_when_too_high`: Very high threshold returns empty vec
- `test_smallest_for_cargo_finds_minimum`: Given ships 50, 100, 200 SCU, finding 75 SCU returns 100 SCU ship
- `test_smallest_for_cargo_none_when_insufficient`: No ship can carry required cargo
- `test_all_ships_returns_all`: Verify all ships accessible

Create a `ShipRegistry::from_ships(ships: Vec<CargoShip>)` test helper if needed, or use `from_api_ships` with mock data.

Pattern: Use `#[allow(clippy::unwrap_used)]` at module level like other test modules.
  </action>
  <verify>cargo test -p intel ships::registry::tests -- --nocapture</verify>
  <done>At least 8 new tests pass for registry lookup functions</done>
</task>

<task type="auto">
  <name>Task 2: Add wikelo/contracts.rs unit tests</name>
  <files>crates/intel/src/wikelo/contracts.rs</files>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of contracts.rs.

**Helper functions:**
- `mock_requirement(item_id: &str, qty: u32)` → ContractRequirement
- `mock_reward(name: &str, value: Option<u64>)` → ContractReward
- `mock_contract(requirements: Vec<ContractRequirement>, rewards: Vec<ContractReward>)` → WikieloContract

**ContractRequirement tests:**
- `test_requirement_new`: Verify constructor sets fields

**ContractReward tests:**
- `test_reward_new`: Default values set correctly
- `test_reward_with_value`: Builder pattern sets estimated_value

**WikieloContract tests:**
- `test_total_reward_value_sum`: Multiple rewards sum correctly
- `test_total_reward_value_none_skipped`: Rewards without value don't break sum
- `test_required_item_ids_unique`: Returns all required item IDs
- `test_requires_item_true`: Item in requirements returns true
- `test_requires_item_false`: Item not in requirements returns false
- `test_quantity_required_single`: Single requirement returns quantity
- `test_quantity_required_multiple`: Same item in multiple requirements sums quantities
- `test_quantity_required_not_found`: Unknown item returns 0

Pattern: Use `#[allow(clippy::unwrap_used)]` at module level.
  </action>
  <verify>cargo test -p intel wikelo::contracts::tests -- --nocapture</verify>
  <done>At least 10 new tests pass for contract types</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p intel` passes all tests
- [ ] `cargo clippy -p intel` has no new warnings
- [ ] Coverage increased (verify with `cargo tarpaulin -p intel --lib --skip-clean`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- At least 18 new tests added to intel crate
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-coverage-to-80/05.2-02-SUMMARY.md`
</output>
