---
phase: 05.2-coverage-to-80
plan: 03
type: execute
---

<objective>
Add comprehensive unit tests for route-graph spatial helper functions and route intersection logic.

Purpose: Cover the complex helper functions in spatial.rs that drive route intersection analysis.
Output: ~100+ additional lines covered in route-graph crate.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@crates/route-graph/src/spatial.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add helper function unit tests</name>
  <files>crates/route-graph/src/spatial.rs</files>
  <action>
Add tests to the existing `#[cfg(test)] mod tests` block for helper functions.

**`extract_system_name` tests:**
- `test_extract_system_name_standard`: "Port Olisar (Stanton > Crusader)" → "Stanton"
- `test_extract_system_name_no_arrow`: "Location (SystemOnly)" → None (no > found)
- `test_extract_system_name_empty`: "" → None
- `test_extract_system_name_no_parens`: "Plain Location" → None

**`extract_short_location` tests:**
- `test_extract_short_location_port_olisar`: Contains "Port Olisar" → Some("Port Olisar")
- `test_extract_short_location_area18`: Contains "Area18" → Some("Area18")
- `test_extract_short_location_case_insensitive`: "HURSTON" → Some("Hurston")
- `test_extract_short_location_not_found`: "Unknown Location" → None

**`generate_intersection_name` tests:**
- `test_generate_name_cross_system`: Routes with different systems → "X Jump Gate"
- `test_generate_name_two_locations`: Two known locations → "Loc1-Loc2 Corridor"
- `test_generate_name_one_location`: One known location → "Loc Junction"
- `test_generate_name_unknown`: No known locations → "Deep Space Intersection"

**`check_if_cross_system` tests:**
- `test_check_cross_system_true`: Route with Stanton→Pyro → true
- `test_check_cross_system_false`: Route within Stanton → false
- `test_check_cross_system_missing_system`: Route with None systems → false

**`infer_system_from_routes` tests:**
- `test_infer_system_single_system`: All routes in Stanton → "Stanton"
- `test_infer_system_cross_system`: Mixed → picks most common
- `test_infer_system_unknown`: No system info → "Unknown"

Use helper function to create RouteSegment for tests:
```rust
fn test_route_segment(origin: &str, dest: &str, origin_sys: Option<&str>, dest_sys: Option<&str>) -> RouteSegment
```
  </action>
  <verify>cargo test -p route-graph spatial::tests -- --nocapture</verify>
  <done>At least 15 new tests pass for helper functions</done>
</task>

<task type="auto">
  <name>Task 2: Add find_route_intersections integration tests</name>
  <files>crates/route-graph/src/spatial.rs</files>
  <action>
Add comprehensive tests for `find_route_intersections` function.

**Tests:**
- `test_find_intersections_two_crossing_routes`: Two routes that cross → 1 intersection
- `test_find_intersections_parallel_routes`: Parallel routes → 0 intersections (too far apart)
- `test_find_intersections_min_routes_filter`: Set min_routes=3, have 2 routes → empty
- `test_find_intersections_proximity_threshold`: Test threshold filtering
- `test_find_intersections_multiple_zones`: 4 routes creating 2 distinct intersection zones
- `test_find_intersections_empty_input`: Empty routes vec → empty result
- `test_intersection_has_correct_fields`: Verify all RouteIntersection fields populated
- `test_intersection_sorted_by_interdiction_value`: Results sorted descending by value

**Helper:**
```rust
fn crossing_routes_pair() -> (RouteSegment, RouteSegment) {
    // Returns two routes known to cross at (5, 5, 0)
}
```

Focus on verifying the core intersection detection logic and field population.
  </action>
  <verify>cargo test -p route-graph spatial::tests -- --nocapture</verify>
  <done>At least 8 new tests pass for find_route_intersections</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p route-graph` passes all tests
- [ ] `cargo clippy -p route-graph` has no new warnings
- [ ] Coverage increased (verify with `cargo tarpaulin -p route-graph --lib --skip-clean`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- At least 23 new tests added to route-graph spatial module
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-coverage-to-80/05.2-03-SUMMARY.md`
</output>
