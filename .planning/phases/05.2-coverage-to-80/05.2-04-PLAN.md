---
phase: 05.2-coverage-to-80
plan: 04
type: execute
---

<objective>
Add unit tests for ships/types.rs and wikelo/types.rs.

Purpose: These modules contain pure value calculation functions with moderate coverage gaps.
Output: ~40+ additional lines covered in intel crate.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@crates/intel/src/ships/types.rs
@crates/intel/src/wikelo/types.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ships/types.rs unit tests</name>
  <files>crates/intel/src/ships/types.rs</files>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of types.rs.

**Helper:**
```rust
fn test_cargo_ship() -> CargoShip {
    CargoShip {
        name: "Test Ship".to_string(),
        manufacturer: "RSI".to_string(),
        cargo_scu: 100,
        crew_size: 2,
        threat_level: 3,
        ship_value_uec: 1_000_000,
        requires_freight_elevator: false,
        quantum_fuel_capacity: 2500.0,
        hydrogen_fuel_capacity: 1000.0,
        qt_drive_size: 2,
        role: ShipRole::Cargo,
        mining_capacity_scu: None,
        mass_kg: Some(50_000.0),
    }
}
```

**CargoShip method tests:**
- `test_salvage_value_with_mass`: Ship with mass_kg calculates hull salvage correctly
- `test_salvage_value_fallback`: Ship without mass_kg uses cargo-based estimate
- `test_estimate_component_value_by_qt_size`: S1, S2, S3 return increasing values
- `test_estimate_component_value_manufacturer_mult`: Drake (0.6x) vs Origin (1.5x)
- `test_interdiction_value_calculation`: cargo 100k, threat 5, crew 2 → expected value
- `test_interdiction_value_single_crew`: No crew penalty for solo ships
- `test_can_complete_route_sufficient_fuel`: Route within range
- `test_can_complete_route_insufficient_fuel`: Route exceeds range
- `test_max_range_mkm`: Known fuel capacity * efficiency

**has_freight_elevator tests:**
- `test_has_freight_elevator_station`: "Port Olisar Station" → true
- `test_has_freight_elevator_arc_l`: "ARC-L1" → true
- `test_has_freight_elevator_surface`: "Lorville" → false
- `test_has_freight_elevator_case_insensitive`: "EVERUS HARBOR" → true

**LootEstimate tests:**
- `test_loot_estimate_disable`: destruction_level=0.0 gives 70% cargo recovery
- `test_loot_estimate_destroy`: destruction_level=1.0 gives ~10% cargo recovery
- `test_loot_estimate_salvage_scales_with_destruction`: Higher destruction = more salvage

Pattern: Use `#[allow(clippy::unwrap_used)]` at module level.
  </action>
  <verify>cargo test -p intel ships::types::tests -- --nocapture</verify>
  <done>At least 15 new tests pass for CargoShip methods and helper functions</done>
</task>

<task type="auto">
  <name>Task 2: Add wikelo/types.rs unit tests</name>
  <files>crates/intel/src/wikelo/types.rs</files>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of types.rs.

First, read the file to understand what types exist. Then add tests for any methods on those types.

Expected content based on coverage report (0/4 lines):
- Likely Display or other trait implementations
- Simple helper methods

**Likely tests needed:**
- Any `Display` impl tests (format string output)
- Any builder pattern methods
- Default value tests
- Serialization round-trip tests if applicable

If the file only contains type definitions without methods, note that in the summary and skip creating tests (derive macros don't need testing).

Pattern: Use `#[allow(clippy::unwrap_used)]` at module level.
  </action>
  <verify>cargo test -p intel wikelo::types::tests -- --nocapture</verify>
  <done>Tests added for any testable methods, or documented that no methods need testing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p intel` passes all tests
- [ ] `cargo clippy -p intel` has no new warnings
- [ ] Coverage increased (verify with `cargo tarpaulin -p intel --lib --skip-clean`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- At least 15 new tests added (primarily from ships/types.rs)
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-coverage-to-80/05.2-04-SUMMARY.md`
</output>
