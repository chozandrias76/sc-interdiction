---
phase: 05.2-coverage-to-80
plan: 05
type: execute
---

<objective>
Add unit tests for remaining route-graph modules: graph.rs, refinery.rs, and locations.rs.

Purpose: Cover remaining pure function gaps in route-graph crate.
Output: ~50+ additional lines covered in route-graph crate.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@crates/route-graph/src/graph.rs
@crates/route-graph/src/refinery.rs
@crates/route-graph/src/locations.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add graph.rs unit tests</name>
  <files>crates/route-graph/src/graph.rs</files>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of graph.rs (or extend existing).

**NodeType tests:**
- `test_node_type_parse_station`: "STATION" → Station
- `test_node_type_parse_outpost`: "OUTPOST" → Outpost
- `test_node_type_parse_landing_zone`: "LANDING_ZONE" → LandingZone
- `test_node_type_parse_city`: "CITY" → City
- `test_node_type_parse_unknown`: "UNKNOWN" → OrbitalMarker (default)
- `test_node_type_parse_case_insensitive`: "station" → Station

**RouteGraph tests:**
- `test_new_creates_empty_graph`: node_count() = 0, edge_count() = 0
- `test_add_station_returns_index`: Add mock station, get valid NodeIndex
- `test_add_station_deduplicates`: Add same station twice, returns same index
- `test_connect_system_adds_edges`: Add multiple nodes in same system, verify edges created
- `test_node_count_and_edge_count`: Verify counts after adding nodes/edges

Create mock Station/Terminal helper:
```rust
fn mock_terminal(code: &str, name: &str, system: &str) -> api_client::Terminal
```

Pattern: Use `#[allow(clippy::unwrap_used)]` at module level.
  </action>
  <verify>cargo test -p route-graph graph::tests -- --nocapture</verify>
  <done>At least 10 new tests pass for NodeType and RouteGraph</done>
</task>

<task type="auto">
  <name>Task 2: Add refinery.rs unit tests</name>
  <files>crates/route-graph/src/refinery.rs</files>
  <action>
Add a `#[cfg(test)] mod tests` block at the end of refinery.rs.

**Static data tests:**
- `test_refinery_methods_count`: REFINERY_METHODS has 3 methods
- `test_refinery_methods_yield_range`: All yields between 0.0 and 1.0
- `test_refinery_methods_ordered_by_time`: Verify processing_time_hours ordering

**RefineryIndex tests:**
- `test_from_terminals_filters_refineries`: Only is_refinery=true included
- `test_from_terminals_empty_input`: Empty vec → empty index
- `test_find_nearest_single_refinery`: Single refinery always returned
- `test_all_refineries_returns_all`: Verify all refineries accessible

Create mock Terminal helper with is_refinery field:
```rust
fn mock_refinery_terminal(id: i64, name: &str, is_refinery: bool) -> api_client::Terminal
```

Pattern: Use `#[allow(clippy::unwrap_used)]` at module level.
  </action>
  <verify>cargo test -p route-graph refinery::tests -- --nocapture</verify>
  <done>At least 7 new tests pass for refinery module</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p route-graph` passes all tests
- [ ] `cargo clippy -p route-graph` has no new warnings
- [ ] Coverage increased (verify with `cargo tarpaulin -p route-graph --lib --skip-clean`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- At least 17 new tests added to route-graph crate
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-coverage-to-80/05.2-05-SUMMARY.md`
</output>
