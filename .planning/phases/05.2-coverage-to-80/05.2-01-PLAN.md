---
phase: 05.2-coverage-to-80
plan: 01
type: execute
---

<objective>
Add unit tests to route-graph crate for spatial.rs and fuel.rs modules.

Purpose: These modules contain pure functions with low coverage (spatial: 13%, fuel: 23%) that are highly testable without mocking.
Output: ~100+ additional lines covered in route-graph crate.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@crates/route-graph/src/spatial.rs
@crates/route-graph/src/fuel.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spatial.rs unit tests</name>
  <files>crates/route-graph/src/spatial.rs</files>
  <action>
Add tests to the existing `#[cfg(test)] mod tests` block for untested functions:

**SpatialIndex tests:**
- `test_from_chokepoints_builds_index`: Create mock Chokepoints with coords, verify index.len()
- `test_find_nearest_returns_sorted`: Insert 3 hotspots, query nearest 2, verify order by distance
- `test_find_within_radius_filters`: Insert hotspots at various distances, verify radius filtering
- `test_find_in_system_case_insensitive`: Test "STANTON" vs "stanton" matching
- `test_by_traffic_sorted_descending`: Insert varied traffic scores, verify sort order

**estimate_position tests:**
- `test_estimate_position_stanton_locations`: Test Hurston, Crusader, ArcCorp, microTech positions
- `test_estimate_position_pyro`: Test Pyro system offset
- `test_estimate_position_unknown_system`: Test fallback to origin

**RouteSegment tests:**
- `test_midpoint_calculation`: Verify midpoint for known segment
- `test_is_cross_system_true`: Different origin/dest systems
- `test_is_cross_system_false`: Same system
- `test_is_cross_system_none`: Missing system info returns false
- `test_length_calculation`: Verify length matches distance_to
- `test_closest_approach_parallel_segments`: Two parallel routes

Use existing test patterns with `#[allow(clippy::unwrap_used)]` at module level.
  </action>
  <verify>cargo test -p route-graph spatial::tests -- --nocapture</verify>
  <done>At least 12 new tests pass, covering SpatialIndex, estimate_position, and RouteSegment</done>
</task>

<task type="auto">
  <name>Task 2: Add fuel.rs unit tests</name>
  <files>crates/route-graph/src/fuel.rs</files>
  <action>
Add tests to the existing `#[cfg(test)] mod tests` block for calculation functions. The file has tests at lines 135, 375, and 753 - find the main public function tests.

**Functions to test:**
- `efficiency_for_size`: Test sizes 1, 2, 3 return correct efficiency, size 0/4 return None
- `calculate_qt_fuel_consumption`: Test known distance * efficiency combinations
- Any other public calculation functions not already covered

Review existing tests first to avoid duplication. Focus on edge cases:
- Zero distance (should return 0 fuel)
- Large distances
- Boundary conditions for fuel capacity checks

Use existing test patterns. Keep tests simple and focused on pure calculation logic.
  </action>
  <verify>cargo test -p route-graph fuel::tests -- --nocapture</verify>
  <done>Fuel calculation tests pass, covering efficiency lookup and consumption calculation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p route-graph` passes all tests
- [ ] `cargo clippy -p route-graph` has no new warnings
- [ ] Coverage increased (verify with `cargo tarpaulin -p route-graph --lib --skip-clean`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- At least 15 new tests added to route-graph
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-coverage-to-80/05.2-01-SUMMARY.md`
</output>
