---
phase: 05.2-coverage-to-80
plan: 07
type: execute
---

<objective>
Add unit tests for intel targets.rs LocationAggregator and remaining helper functions.

Purpose: Cover the untested helper code in targets.rs that doesn't require API mocking.
Output: ~50+ additional lines covered in intel crate.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@crates/intel/src/targets.rs
@crates/intel/src/targets_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LocationAggregator unit tests</name>
  <files>crates/intel/src/targets_tests.rs</files>
  <action>
Add tests for the `LocationAggregator` helper struct (lines 600-700 in targets.rs).

**Tests to add:**
- `test_location_aggregator_new`: Verify initial state
- `test_location_aggregator_add_route`: Add routes and verify accumulation
- `test_location_aggregator_multiple_routes`: Multiple commodities and ships
- `test_location_aggregator_into_hotspot_empty`: Empty aggregator → default values
- `test_location_aggregator_into_hotspot_with_data`: Full data → correct hotspot
- `test_location_aggregator_avg_threat_calculation`: Verify threat averaging
- `test_location_aggregator_top_commodities_sorted`: Top 5 by value
- `test_location_aggregator_likely_ships_sorted`: Top 5 by count

**Pattern:**
```rust
#[test]
fn test_location_aggregator_new() {
    let agg = LocationAggregator::new("Port Olisar".to_string(), "Stanton".to_string());
    assert_eq!(agg.location, "Port Olisar");
    assert_eq!(agg.system, "Stanton");
    assert_eq!(agg.total_value, 0.0);
    assert_eq!(agg.route_count, 0);
}
```
  </action>
  <verify>cargo test -p intel targets_tests::test_location_aggregator -- --nocapture</verify>
  <done>At least 8 new LocationAggregator tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add HotRoute and TradeRun struct tests</name>
  <files>crates/intel/src/targets_tests.rs</files>
  <action>
Add tests for the data structures in targets.rs to improve coverage.

**Tests to add:**
- `test_hot_route_serialization`: Verify HotRoute serializes correctly
- `test_trade_run_serialization`: Verify TradeRun serializes correctly
- `test_route_leg_fields`: Verify RouteLeg field population
- `test_interdiction_hotspot_suggested_position`: Test position text generation
- `test_commodity_value_struct`: Test CommodityValue fields
- `test_ship_frequency_struct`: Test ShipFrequency fields

**Focus on testing the struct creation and serialization paths that aren't
covered by the async integration tests.**
  </action>
  <verify>cargo test -p intel targets_tests:: -- --nocapture</verify>
  <done>At least 6 new struct tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p intel` passes all tests
- [ ] `cargo clippy -p intel` has no new warnings
- [ ] Coverage increased (verify with `cargo tarpaulin -p intel --lib --skip-clean`)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- At least 14 new tests added to intel crate
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-coverage-to-80/05.2-07-SUMMARY.md`
</output>
