---
phase: 11-demand-intel-integration
plan: 01
type: execute
---

<objective>
Add demand scoring to TargetAnalyzer — flag ships heading TO Wikelo turn-in locations and enrich output structs with contract demand data.

Purpose: Complement existing source flagging (Phase 4) with demand-side intel. Source flagging answers "they might be carrying Wikelo items" (departing from source locations). Demand scoring answers "they're heading somewhere that accepts Wikelo items" (arriving at turn-in locations). Together they complete the interdiction picture.

Output: TargetAnalyzer enriched with ContractRegistry, demand fields on TargetPrediction/HotRoute/InterdictionHotspot, and unit tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (auto-selected from dependency graph):
@.planning/phases/10-demand-registry/10-01-SUMMARY.md
@.planning/phases/04-source-intel-integration/04-02-SUMMARY.md
@.planning/phases/04-source-intel-integration/04-03-SUMMARY.md

# Key source files:
@crates/intel/src/targets.rs
@crates/intel/src/targets_tests.rs
@crates/intel/src/wikelo/intel.rs
@crates/intel/src/wikelo/contract_registry.rs
@crates/intel/src/wikelo/mod.rs

**Tech stack available:** ContractRegistry with bidirectional indexes, Arc<WikieloIntel> builder pattern, calculate_wikelo_score() helper
**Established patterns:**
- Builder pattern with_wikelo() for optional TargetAnalyzer dependencies (04-02)
- Option<f64> scoring with None=unconfigured, Some(0)=no match (04-03)
- Wikelo scoring returns (Option<f64>, Vec<String>) tuple (04-03)
**Constraining decisions:**
- Phase 4: Only flag departing targets for source intel (arriving already have cargo)
- Phase 4: wikelo_flag is Option<SourceFlag> for backward compatibility
- Phase 4: Same scoring formula for HotRoute and InterdictionHotspot
- Phase 10: ContractRegistry follows WikieloRegistry pattern, reuses normalize_location
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ContractRegistry to WikieloIntel and TargetAnalyzer, add demand fields to output structs</name>
  <files>crates/intel/src/wikelo/intel.rs, crates/intel/src/targets.rs, crates/intel/src/wikelo/mod.rs</files>
  <action>
1. **WikieloIntel** (intel.rs): Add `contract_registry: Arc<ContractRegistry>` field. Update `new()` to accept it, update `from_static()` to create one from `ContractRegistry::new()`. Add accessor method `contracts(&self) -> &ContractRegistry`. Add `flag_demand_at_location(&self, location: &str) -> Option<DemandFlag>` that checks `contracts_at_location()` — returns None if no contracts, otherwise returns a DemandFlag with contract count and names.

2. **DemandFlag** (intel.rs): New struct with fields: `location: String`, `contract_count: usize`, `top_contracts: Vec<DemandContractSummary>` (up to 5), `has_high_value: bool`. Also add `DemandContractSummary` with `name: String`, `category: ContractCategory`, `reward_value: Option<u64>`. Both derive Debug, Clone, Serialize.

3. **TargetPrediction** (targets.rs): Add `demand_flag: Option<DemandFlag>` field. This flags arriving targets heading to Wikelo turn-in locations (the complement of wikelo_flag which flags departing targets from source locations).

4. **HotRoute** (targets.rs): Add `demand_score: Option<f64>` and `demand_contracts: Vec<String>` fields for destination demand scoring (parallels wikelo_score/wikelo_items which score origin supply).

5. **InterdictionHotspot** (targets.rs): Add `demand_score: Option<f64>` and `demand_contracts: Vec<String>` fields.

6. **calculate_demand_score()** (targets.rs): New helper function matching calculate_wikelo_score pattern. Signature: `fn calculate_demand_score(wikelo: &Option<Arc<WikieloIntel>>, location: &str) -> (Option<f64>, Vec<String>)`. Returns None if no WikieloIntel. Scoring: 20 base if contracts exist at location, +15 per contract with reward value > 10,000, +5 per contract (capped at 50 bonus), cap total at 100. Collect contract names.

7. **Populate demand fields** in TargetAnalyzer methods:
   - `predict_targets_at()`: Set `demand_flag` for arriving targets (when `!is_departing`) by calling `wikelo.flag_demand_at_location()` on the origin location (where the target came from — this is the turn-in location perspective). Actually, rethink: for arriving targets at location X, location X IS the potential turn-in location. So flag arriving targets with demand at the current location. For departing targets heading to destination, flag if destination has demand. Use `flag_demand_at_location` on the destination for departing targets, and on location (the analysis location) for arriving targets.
   - `get_hot_routes()`: Calculate demand_score/demand_contracts for destination using `calculate_demand_score()`.
   - `get_interdiction_hotspots()`: Calculate demand_score/demand_contracts for each location using `calculate_demand_score()`.

8. **Update existing struct constructors** in test helpers (targets_tests.rs, TUI ui.rs) that construct HotRoute/InterdictionHotspot/TargetPrediction — add the new fields with default values (None/empty vec) to maintain compilation. Check all files that construct these structs.

AVOID: Don't modify the existing wikelo_flag/wikelo_score/wikelo_items fields — demand is additive, not replacing source intel. Don't change the calculate_wikelo_score function. Don't break backward compatibility — all new fields should be Option or Vec (defaultable).
  </action>
  <verify>cargo build --workspace passes with no errors. cargo clippy --workspace -- -D warnings passes clean.</verify>
  <done>WikieloIntel has ContractRegistry, all output structs have demand fields, calculate_demand_score helper exists, all TargetAnalyzer methods populate demand fields, existing tests still compile.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for demand scoring integration</name>
  <files>crates/intel/src/targets_tests.rs, crates/intel/src/wikelo/intel.rs</files>
  <action>
1. **WikieloIntel tests** (intel.rs): Add tests for `flag_demand_at_location()`:
   - `test_flag_demand_at_wikelo_station`: Use `WikieloIntel::from_static()`, check `flag_demand_at_location("Wikelo Emporium Dasi")` returns Some with contract_count > 0.
   - `test_flag_demand_at_non_wikelo_location`: Check that a random location returns None.

2. **calculate_demand_score tests** (targets_tests.rs):
   - `test_demand_score_none_without_wikelo`: Pass None WikieloIntel, verify returns (None, empty vec).
   - `test_demand_score_zero_at_non_demand_location`: Pass WikieloIntel with real data, check random location returns (Some(0.0), empty vec).
   - `test_demand_score_positive_at_wikelo_station`: Pass WikieloIntel from_static, check "Wikelo Emporium Dasi" returns Some(score > 0) and non-empty contract names.
   - `test_demand_score_capped_at_100`: Verify score never exceeds 100.

3. Run full test suite to verify nothing is broken.
  </action>
  <verify>cargo test --workspace passes. All new tests pass. Total test count increases (was 194).</verify>
  <done>At least 4 new demand scoring tests pass. All 194+ existing tests still pass. cargo clippy clean.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cargo build --workspace` succeeds
- [ ] `cargo clippy --workspace -- -D warnings` is clean
- [ ] `cargo test --workspace` passes all tests (194+ existing + new demand tests)
- [ ] WikieloIntel::from_static() includes ContractRegistry
- [ ] TargetPrediction has demand_flag field
- [ ] HotRoute has demand_score and demand_contracts fields
- [ ] InterdictionHotspot has demand_score and demand_contracts fields
- [ ] calculate_demand_score returns scores for Wikelo turn-in locations
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Demand scoring complements source scoring (supply + demand intel)
- Backward compatibility maintained (all new fields are Option or Vec)
- Phase 11 complete, ready for Phase 12 (CLI/TUI Demand Views)
</success_criteria>

<output>
After completion, create `.planning/phases/11-demand-intel-integration/11-01-SUMMARY.md`
</output>
