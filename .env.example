# PostgreSQL connection string (matches docker-compose.yml)
DATABASE_URL=postgres://sc:sc@localhost:5432/sc_interdiction

# Path to SCLogistics repository (for data import)
SCLOGISTICS_PATH=../SCLogistics
